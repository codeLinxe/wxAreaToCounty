{
  "name": "wx_area_json",
  "version": "1.0.0",
  "description": "1. npm init \r 2. npm install -g live-server\r 3. npm i node-fetch --save",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cross-blob": "^2.0.0",
    "node-fetch": "^2.6.1"
  }
}
